# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8
statsmodels>=0.14

# Excel I/O
openpyxl>=3.1

# Model inference (required only for benchmark/evaluate_benchmark.py)
torch>=2.2
transformers>=4.40
peft>=0.10
tqdm>=4.66
requests>=2.31

# Python version requirement: >= 3.9
#
# To install all dependencies (excluding torch/transformers for analysis-only use):
#   pip install numpy pandas scipy matplotlib statsmodels openpyxl
#
# To install the full set including model inference:
#   pip install -r requirements.txt
#
# For reproducibility, pin exact versions used in the paper's environment:
#   pip freeze > requirements_pinned.txt
